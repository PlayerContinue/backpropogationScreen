<UserControl x:Class="VisualBackPropogation.Custom_Controls.True_False_Button"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="100">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50"></ColumnDefinition>
            <ColumnDefinition Width="50"></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <RadioButton GroupName="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=GroupName}" Grid.Column="0"
             IsChecked="{Binding PropertyValue}" Content="True" Name="TrueButton"/>
        <RadioButton Name="FalseButton" GroupName="{Binding RelativeSource={RelativeSource AncestorType={x:Type UserControl}}, Path=GroupName}"  Content="False" Grid.Column="1"
             IsChecked="{Binding PropertyValue, 
                         Converter={StaticResource BoolInverterConverter}}" />
    </Grid>
</UserControl>
